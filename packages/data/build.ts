// SteelCompendium ingest: pulls the SDK at the pinned version, normalizes statblocks
// and abilities, parses effect text, validates against Zod schemas in @ironyard/shared,
// emits typed JSON to apps/web/public/data/.
//
// Real implementation lands later in Phase 0 / Phase 1 (pending user confirmation).
console.log('build:data — not yet implemented');
